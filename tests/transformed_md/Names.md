<!-- markdown-doctest-names: name -->
```rust
single name
```

<!-- markdown-doctest-names: name,another-one -->
```rust
multiple names
```

- <!-- markdown-doctest-names: list -->
    ```rust
    list
    ```

```rust
ensure that previous names were cleared; this should have no name
```
